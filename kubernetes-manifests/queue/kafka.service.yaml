apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: spring-cloud-config
  labels:
    name: kafka
spec:
  ports:
    - port: 9092
      name: kafka-port
      protocol: TCP
  selector:
    app: kafka
    id: "0"
  type: LoadBalancer
